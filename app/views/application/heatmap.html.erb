<!DOCTYPE html>
<html>
  <head>
    <title>HivePulse</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=Edge;chrome=1" >
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false&key=<%= Rails.application.secrets.google_maps_api_key %>"></script>
    <%= analytics_init if Rails.env.production? %>
    <%= javascript_include_tag 'application' %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>" > 
    <div id="tog"class="btn">Toggle Pulse</div>
    <div id="heatmapArea" class="well" /></div>
    <div id="footer" class="twubbles-footer navbar-fixed-bottom">
    <div class="container hivepulse-footer">
      <div class="row">
        <div id="hivepulse-footer">
          <p>Twitter users love to tweet about where they are, what they are doing, and new places they’ve discovered. What if you could aggregates the stream into a real-time heatmap of the action.<br><span>HivePulse is your visual source for feeling the heartbeat of the city.</span></p>
        </div>
      </div>
    </div>
  </body>
</html>